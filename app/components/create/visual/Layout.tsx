'use client';

import SidebarTitle from "../../sidebar/SidebarTitle";

const Layout = () => {
    return (
        <div>
            <SidebarTitle bigger label="Layout"/>
        </div>
    )
}

export default Layout;